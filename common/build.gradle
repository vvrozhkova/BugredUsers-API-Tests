plugins {
    id "io.freefair.lombok" version "6.4.2"
}

dependencies {
    implementation(
            deps.allureRestAssured,
            deps.lombok,
            deps.jacksonDatabind,
    )
}

jar.enabled=true